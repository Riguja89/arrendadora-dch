/** Placeholder de dashboard — sin datos reales todavía. */
export function DashboardPage() {
  return (
    <section>
      <h2>Dashboard</h2>
      <p>Resumen operativo del panel — pendiente de implementar.</p>
    </section>
  );
}
