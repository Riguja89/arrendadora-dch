/**
 * Placeholder de gestión de propiedades — sin fetch real todavía (bounded context
 * `admin-propiedades`, contrato `/admin/propiedades`, pendiente de Construir). Ver
 * `src/lib/http-client.ts` para el cliente base tipado con `@arrendadora/shared`.
 */
export function PropiedadesPage() {
  return (
    <section>
      <h2>Propiedades</h2>
      <p>Listado y gestión de propiedades — pendiente de implementar.</p>
    </section>
  );
}
