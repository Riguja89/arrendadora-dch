/**
 * Placeholder de gestión de usuarios — solo alcanzable por rol Administrador
 * (`RequireRole roles={["administrador"]}` en `App.tsx`, matriz ADR-014). CRUD de usuarios
 * (CU-003/CU-004 de la spec de auth-usuarios) pendiente de un próximo incremento.
 */
export function UsuariosPage() {
  return (
    <section>
      <h2>Usuarios</h2>
      <p>Gestión de usuarios y roles — pendiente de implementar (próximo incremento).</p>
    </section>
  );
}
