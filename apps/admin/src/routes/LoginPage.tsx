/**
 * Placeholder de login — SIN autenticación real todavía (bounded context `auth-usuarios`,
 * `apps/api/src/modules/auth-usuarios`, pendiente de Construir).
 */
export function LoginPage() {
  return (
    <main className="login-page">
      <h1>Arrendadora — Panel admin</h1>
      <p>Formulario de acceso pendiente de implementar (placeholder de scaffolding).</p>
    </main>
  );
}
